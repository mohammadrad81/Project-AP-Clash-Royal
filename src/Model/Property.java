package Model;

/**
 * a tagging interface for better implementation
 * for those entities that are set in the arena ( do not move )
 * like towers and buildings
 */
public interface Property {
    // just a tagging interface
}
